{
  "name": "ts-dp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "nodemon ./src/index.ts",
    "deWatch": "nodemon ./src/decorator/decorator.ts",
    "build": "tsc",
    "dev": "npm run build && concurrently \" npm run watch \" \" npx tsc --watch \"",
    "ddev": "npm run build && concurrently \" npm run deWatch \" \" npx tsc --watch \""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.0",
    "nodemon": "^3.1.7",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
